import { createPageMetadata } from "@/lib/page-shell";

export const metadata = createPageMetadata({
  title: "清蓮の強み",
  description: "株式会社清蓮の強み（再構築準備中）",
  path: "/strength",
});

export default function StrengthPage() {
  return (
    <main id="main-content">
      <h1>清蓮の強み</h1>
      <p>このページは再構築準備中です。</p>
    </main>
  );
}

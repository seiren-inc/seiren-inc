# 清蓮コーポレートサイト

> 🚧 **再構築準備中 / Rebuild in progress**

このリポジトリの `main` ブランチは、サイト再構築のためにクリーンスレート（初期化）されています。
既存サイトの実装コードは削除済みです。

## 現在の状態

- 既存サイト関連ファイル（`src/`, `public/`, 各種設定ファイル等）は削除済み
- 再構築に向けた準備段階

## バックアップ

初期化前の状態は以下のブランチに完全保存されています。**削除しないでください。**

```
backup-before-reset
```

復元が必要な場合はこのブランチを参照してください。

## 想定スタック（再構築時）

| 項目 | 内容 |
|------|------|
| Framework | Next.js 15 (App Router) |
| React | 19 |
| Styling | Tailwind CSS v3 |
| Package Manager | npm |

詳細は `CLAUDE.md` および `.claude/rules/` を参照してください。

import { createPageMetadata } from "@/lib/page-shell";

export const metadata = createPageMetadata({
  title: "利用規約",
  description: "株式会社清蓮の利用規約（再構築準備中）",
  path: "/terms",
});

export default function TermsPage() {
  return (
    <main id="main-content">
      <h1>利用規約</h1>
      <p>このページは再構築準備中です。</p>
    </main>
  );
}

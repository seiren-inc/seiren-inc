import { createPageMetadata } from "@/lib/page-shell";

export const metadata = createPageMetadata({
  title: "会社情報",
  description: "株式会社清蓮の会社情報（再構築準備中）",
  path: "/company",
});

export default function CompanyPage() {
  return (
    <main id="main-content">
      <h1>会社情報</h1>
      <p>このページは再構築準備中です。</p>
    </main>
  );
}

import { createPageMetadata } from "@/lib/page-shell";

export const metadata = createPageMetadata({
  title: "事業内容",
  description: "株式会社清蓮の事業内容（再構築準備中）",
  path: "/business",
});

export default function BusinessPage() {
  return (
    <main id="main-content">
      <h1>事業内容</h1>
      <p>このページは再構築準備中です。</p>
    </main>
  );
}

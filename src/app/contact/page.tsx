import { createPageMetadata } from "@/lib/page-shell";

export const metadata = createPageMetadata({
  title: "お問い合わせ",
  description: "株式会社清蓮へのお問い合わせ（再構築準備中）",
  path: "/contact",
});

export default function ContactPage() {
  return (
    <main id="main-content">
      <h1>お問い合わせ</h1>
      <p>このページは再構築準備中です。</p>
    </main>
  );
}

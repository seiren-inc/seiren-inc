import { createPageMetadata } from "@/lib/page-shell";

export const metadata = createPageMetadata({
  title: "パートナー",
  description: "株式会社清蓮のパートナー情報（再構築準備中）",
  path: "/partner",
});

export default function PartnerPage() {
  return (
    <main id="main-content">
      <h1>パートナー</h1>
      <p>このページは再構築準備中です。</p>
    </main>
  );
}
